// Ported from Rust to C++ by Claude Sonnet 5.

// Copyright 2012-2026 The Rust Project Developers. See the COPYRIGHT
// file at the top-level directory of this distribution and at
// http://rust-lang.org/COPYRIGHT.
//
// Licensed under the Apache License, Version 2.0 <LICENSE-APACHE or
// http://www.apache.org/licenses/LICENSE-2.0> or the MIT license
// <LICENSE-MIT or http://opensource.org/licenses/MIT>, at your
// option. This file may not be copied, modified, or distributed
// except according to those terms.

// NOTE: The following code was generated by "scripts/unicode-cpp.py", do not edit directly
#pragma once
#include <cstdint>
#include <cstddef>
#include "../props.hpp" // for RangePair

namespace unicode_width {

// The version of Unicode (http://www.unicode.org/) that this table data
// is based on.
constexpr std::uint8_t UNICODE_VERSION[3] = { 17, 0, 0 };

// Autogenerated. 1 sub-table(s). Consult lookup_width() for layout info.
extern const std::uint8_t WIDTH_ROOT[256];

// Autogenerated. 16 sub-table(s). Consult lookup_width() for layout info.
extern const std::uint8_t WIDTH_ROOT_CJK[256];

constexpr std::size_t WIDTH_MIDDLE_LEN = 20;
// Autogenerated. 4 sub-table(s). Consult lookup_width() for layout info.
// NOTE: rows [0, primary rows) serve non-CJK lookups too; the remaining
// rows (present in the Rust crate only when the "cjk" feature is enabled)
// are always included here since lookup_width() (non-CJK) never indexes
// past its own primary range.
extern const std::uint8_t WIDTH_MIDDLE[WIDTH_MIDDLE_LEN][64];

constexpr std::size_t WIDTH_LEAVES_LEN = 186;
// Autogenerated. 186 sub-table(s). Consult lookup_width() for layout info.
// NOTE: rows [0, primary rows) serve non-CJK lookups too; the remaining
// rows (present in the Rust crate only when the "cjk" feature is enabled)
// are always included here since lookup_width() (non-CJK) never indexes
// past its own primary range.
extern const std::uint8_t WIDTH_LEAVES[WIDTH_LEAVES_LEN][32];

// A closed codepoint range [lo, hi].
struct CodepointRange {
    std::uint32_t lo;
    std::uint32_t hi;
};

// Sorted list of codepoint ranges (inclusive) that are zero-width but not
// Joining_Type=Transparent.
constexpr std::size_t NON_TRANSPARENT_ZERO_WIDTHS_LEN = 71;
extern const CodepointRange NON_TRANSPARENT_ZERO_WIDTHS[NON_TRANSPARENT_ZERO_WIDTHS_LEN];

// Sorted list of codepoint ranges (inclusive) that don't affect how the
// combining solidus applies (mostly ccc > 1). Only consulted in CJK mode.
constexpr std::size_t SOLIDUS_TRANSPARENT_LEN = 208;
extern const CodepointRange SOLIDUS_TRANSPARENT[SOLIDUS_TRANSPARENT_LEN];

// Array of 1024-bit bitmaps. Index into the correct bitmap with the 10 LSB
// of your codepoint to get whether it can start an emoji presentation
// sequence.
extern const std::uint8_t EMOJI_PRESENTATION_LEAVES[7][128];

extern const RangePair TEXT_PRESENTATION_LEAF_0[4];
extern const RangePair TEXT_PRESENTATION_LEAF_1[1];
extern const RangePair TEXT_PRESENTATION_LEAF_2[15];
extern const RangePair TEXT_PRESENTATION_LEAF_3[10];
extern const RangePair TEXT_PRESENTATION_LEAF_4[3];
extern const RangePair TEXT_PRESENTATION_LEAF_5[1];
extern const RangePair TEXT_PRESENTATION_LEAF_6[13];
extern const RangePair TEXT_PRESENTATION_LEAF_7[22];
extern const RangePair TEXT_PRESENTATION_LEAF_8[4];
extern const RangePair TEXT_PRESENTATION_LEAF_9[10];
extern const RangePair EMOJI_MODIFIER_LEAF_0[2];
extern const RangePair EMOJI_MODIFIER_LEAF_1[1];
extern const RangePair EMOJI_MODIFIER_LEAF_2[4];
extern const RangePair EMOJI_MODIFIER_LEAF_3[9];
extern const RangePair EMOJI_MODIFIER_LEAF_4[4];
extern const RangePair EMOJI_MODIFIER_LEAF_5[6];
extern const RangePair EMOJI_MODIFIER_LEAF_6[12];
extern const RangePair EMOJI_MODIFIER_LEAF_7[2];
} // namespace unicode_width

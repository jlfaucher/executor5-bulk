<chapter id="hostemu">
<title>Host Emulator (HostEmu)</title>
<para>HostEmu is a subcommand environment that partially emulates a TSO/CMS environment. It
provides a small subset of commands avaliable in those environments which make the
transition from a real host Rexx programming environment to a Linux/Windows ooRexx environment much
easier. The following subcommands are available:</para>
<variablelist>
    <varlistentry>
        <term><filename>EXECIO</filename></term>
        <listitem><para>an I/O mechanism.</para></listitem>
    </varlistentry>
    <varlistentry>
        <term><filename>HI</filename></term>
        <listitem><para>halts the current Rexx program.</para></listitem>
    </varlistentry>
    <varlistentry>
        <term><filename>TE</filename></term>
        <listitem><para>deactivate the Rexx trace mechanism.</para></listitem>
    </varlistentry>
    <varlistentry>
        <term><filename>TS</filename></term>
        <listitem><para>activate the Rexx trace mechanism.</para></listitem>
    </varlistentry>
</variablelist>
<para>The HostEmu HI, TS, TE commands have no arguments that are acceptable in the HostEmu
environment. Thus their syntax is very simple. The EXECIO subcommand is more complicated
and requires some explanation. It is a simplified version of the mainframe command but
will provide most of the functionality you will need on a daily basis.</para>
<para>To include and use the HostEmu subcommand environment you must place a ooRexx directive
in your script. The following shows how to accomplish this.</para>
<programlisting>
::requires "hostemu" LIBRARY
</programlisting>
<para>This will activate the environment. The subcommand name is "HostEmu" (the case of this
string is not important). You can send commands to this environment via the ooRexx
address statement. Here is an example.</para>
<programlisting>
address hostemu 'execio * diskr "./inputfile.txt" (finis stem in.'
</programlisting>
<para>Note that the file name MUST be placed within a set of quotation marks.</para>
<para>The example above should look very familiar to a mainframe Rexx programmer. The big 
difference is that a real file name is used instead of a DDNAME and the HostEmu
environment is not the default address environment, thus the requirement that you either
include the 'HostEmu' environment name in the address statement or you make the 'HostEmu'
environment the default environment.</para>

<section id="execio">
<indexterm><primary>HostEmu subcommands</primary>
<secondary>EXECIO</secondary></indexterm>
<indexterm><primary>EXECIO subcommand</primary>
<secondary>hostemu</secondary></indexterm>
<title>EXECIO subcommand</title>
<programlisting>
>>--EXECIO--+--num--+--+--DISKR--+--"filename"--(--+--FINIS STEM stemname.--+--><
            +--*----+  +--DISKW--+                 +--STEM stemname. FINIS--+
                                                   +--STEM stemname.--------+
                                                   +--FINIS--+--LIFO--+-----+
                                                             +--FIFO--+
                                                             +--SKIP--+
</programlisting>
<section>
<title>Command Options</title>
<variablelist>
    <varlistentry>
        <term>num</term>
        <listitem><para>Specifies the number of records (text lines) to read or write.</para></listitem>
    </varlistentry>
    <varlistentry>
        <term>*</term>
        <listitem><para>Specifies that all remaining records are to be read or written.</para></listitem>
    </varlistentry>
    <varlistentry>
        <term>DISKR</term>
        <listitem><para>The operation is a disk read operation.</para></listitem>
    </varlistentry>
    <varlistentry>
        <term>DISKW</term>
        <listitem><para>The operation os a disk write operation.</para></listitem>
    </varlistentry>
    <varlistentry>
        <term>filename</term>
        <listitem><para>The name of the file for the disk operation. This option MUST be enclosed in double quotes.
        </para></listitem>
    </varlistentry>
    <varlistentry>
        <term>FINIS</term>
        <listitem><para>The file will be closed at the end of the operation.</para></listitem>
    </varlistentry>
    <varlistentry>
        <term>STEM stemname.</term>
        <listitem><para>The operation is works against the specified stem. A trailing period is required or
       the name will be used as the root of a standard Rexx variable name.</para></listitem>
    </varlistentry>
    <varlistentry>
        <term>LIFO</term>
        <listitem><para>Specifies that instead of a Rexx stem The Rexx SESSION queue should be used. The queue
        will be read or written to using LIFO.</para></listitem>
    </varlistentry>
    <varlistentry>
        <term>FIFO</term>
        <listitem><para>Specifies that instead of a Rexx stem The Rexx SESSION queue should be used. The queue
        will be read or written to using FIFO.</para></listitem>
    </varlistentry>
    <varlistentry>
        <term>SKIP</term>
        <listitem><para>Specifies the number of records (text lines) to be skipped. No stem or queue
        operations will be performed in this case.</para></listitem>
    </varlistentry>
</variablelist>
<para>Note that the LIFO, FIFO and SKIP options are not valid for DISKW operations.</para>
<para>These options should provide almost all of the EXECIO operations you will need in the
non-mainframe operating system environment.</para>
</section>
</section>

<section id="hi">
<indexterm><primary>HostEmu subcommands</primary>
<secondary>HI</secondary></indexterm>
<indexterm><primary>HI subcommand</primary>
<secondary>hostemu</secondary></indexterm>
<title>HI subcommand</title>
<programlisting>
>>--HI-------------------------------------------------------------------------><
</programlisting>
<para>Halts the current Rexx program.</para>
</section>

<section id="te">
<indexterm><primary>HostEmu subcommands</primary>
<secondary>TE</secondary></indexterm>
<indexterm><primary>TE subcommand</primary>
<secondary>hostemu</secondary></indexterm>
<title>TE subcommand</title>
<programlisting>
>>--TE-------------------------------------------------------------------------><
</programlisting>
<para>Deactivate the Rexx trace mechanism.</para>
</section>

<section id="ts">
<indexterm><primary>HostEmu subcommands</primary>
<secondary>TS</secondary></indexterm>
<indexterm><primary>TS subcommand</primary>
<secondary>hostemu</secondary></indexterm>
<title>TS subcommand</title>
<programlisting>
>>--TS-------------------------------------------------------------------------><
</programlisting>
<para>Activate the Rexx trace mechanism.</para>
</section>

</chapter>

